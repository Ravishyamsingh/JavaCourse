{
  "version": 2,
  "buildCommand": "cd workspace/shadcn-ui && npm install && npm run build",
  "outputDirectory": "workspace/shadcn-ui/dist",
  "framework": "vite",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
